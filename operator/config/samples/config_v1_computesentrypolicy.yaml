apiVersion: config.aiguard.io/v1
kind: ComputeSentryPolicy
metadata:
  labels:
    app.kubernetes.io/name: operator
    app.kubernetes.io/managed-by: kustomize
  name: computesentrypolicy-sample
spec:
  # TODO(user): Add fields here
